[package]
name = "usher"
version = "0.1.0"
edition = "2024"

[dependencies]
enumset = "1.1.7"
pretty_assertions = "1.4.1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
